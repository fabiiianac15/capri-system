import { useEffect, useState } from 'react';
import { useAuth } from '../context/AuthContext';
import { useNavigate } from 'react-router-dom';
import goatService from '../services/goat.service';
import type { GoatStats } from '../types/index';

export default function Dashboard() {
  const { logout } = useAuth();
  const navigate = useNavigate();
  const [stats, setStats] = useState<GoatStats | null>(null);
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    loadStats();
  }, []);

  const loadStats = async () => {
    try {
      const data = await goatService.getStats();
      setStats(data);
    } catch (error) {
      console.error('Error al cargar estadísticas:', error);
    } finally {
      setIsLoading(false);
    }
  };

  const handleLogout = () => {
    logout();
    navigate('/login');
  };

  if (isLoading) {
    return (
      <div className="flex h-screen w-full items-center justify-center bg-[#e8f0d8]">
        <div className="flex flex-col items-center gap-4">
          <svg className="h-12 w-12 animate-spin text-[#6b7c45]" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" fill="none"></circle>
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
          <p className="text-xl font-medium text-[#6b7c45]">Verificando acceso...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen flex flex-col bg-[#e8f0d8]">
      {/* HEADER GRANME */}
      <header className="bg-[#6b7c45] text-white p-4 sticky top-0 z-50 shadow-md">
        <div className="max-w-7xl mx-auto flex items-center justify-between">
          <div className="flex items-center gap-4">
            <div className="flex items-center gap-3">
              <div className="bg-[#5a6a3a] p-2 rounded-lg">
                <span className="text-3xl">🐐</span>
              </div>
              <div>
                <h1 className="text-xl font-bold hidden md:block">Sistema de Gestión Granme</h1>
                <h1 className="text-xl font-bold md:hidden">Granme</h1>
                <p className="text-xs text-white/80 hidden md:block">Sistema Profesional de Gestión Caprina</p>
              </div>
            </div>
          </div>
          
          <div className="flex items-center gap-3">
            <button
              onClick={() => navigate('/goats')}
              className="px-4 py-2 bg-[#5a6a3a] hover:bg-[#1a2e02] text-white rounded-md font-semibold text-sm transition-all duration-300 flex items-center gap-2"
            >
              <span className="hidden md:inline">Gestión de Cabras</span>
              <span className="md:hidden">🐐</span>
            </button>
            <div className="text-right hidden md:block bg-[#5a6a3a] px-4 py-2 rounded-lg">
              <p className="text-sm font-bold">Administrador CAPRI</p>
              <p className="text-xs text-white/80">ADMIN</p>
            </div>
            <button
              onClick={handleLogout}
              className="px-4 py-2 bg-[#1a2e02] hover:bg-[#2a4a04] text-white rounded-md font-semibold text-sm transition-all duration-300 flex items-center gap-2"
            >
              <span className="hidden md:inline">Cerrar Sesión</span>
              <span>🚪</span>
            </button>
          </div>
        </div>
      </header>

      {/* MAIN CONTENT */}
      <main className="flex-1 p-6 overflow-auto">
        <div className="max-w-7xl mx-auto">
          {/* Welcome Banner */}
          <div className="mb-8 bg-white rounded-xl shadow-sm p-8 border border-[#E8E8E8] animate-fade-in">
            <h2 className="text-3xl font-bold text-[#1a2e02] mb-2 flex items-center gap-3">
              ¡Bienvenido, Administrador CAPRI!
              <span className="animate-wave inline-block text-3xl">👋</span>
            </h2>
            <p className="text-[#6b7c45] text-lg font-medium mb-4">
              Aquí está el resumen actualizado de tu granja caprina 🌟
            </p>
            <div className="flex items-center gap-2 text-sm text-earth-600">
              <span>📅</span>
              <span className="font-medium">sábado, 11 de octubre de 2025</span>
            </div>
          </div>

          {/* Stats Cards Grid */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            {/* Card 1: Total Cabras */}
            <div className="bg-white rounded-xl shadow-sm p-6 border border-[#E8E8E8] hover:shadow-md transition-all duration-300 animate-fade-in">
              <div className="flex items-center justify-between mb-2">
                <p className="text-sm font-semibold text-earth-500 uppercase tracking-wide">Total Cabras</p>
                <div className="bg-[#d3dbb8] p-2 rounded-lg">
                  <span className="text-2xl">🐐</span>
                </div>
              </div>
              <p className="text-4xl font-bold text-[#1a2e02] my-3">
                {stats?.total || 0}
              </p>
              <p className="text-xs text-earth-500 flex items-center gap-1">
                <span className="w-1.5 h-1.5 bg-[#6b7c45] rounded-full"></span>
                Activas en el sistema
              </p>
            </div>

            {/* Card 2: Producción */}
            <div className="bg-white rounded-xl shadow-sm p-6 border border-[#E8E8E8] hover:shadow-md transition-all duration-300 animate-fade-in">
              <div className="flex items-center justify-between mb-2">
                <p className="text-sm font-semibold text-earth-500 uppercase tracking-wide">Producción Total</p>
                <div className="bg-blue-50 p-2 rounded-lg">
                  <span className="text-2xl">🥛</span>
                </div>
              </div>
              <p className="text-4xl font-bold text-blue-700 my-3">
                {stats?.totalMilkProduction.toFixed(1) || 0}L
              </p>
              <p className="text-xs text-earth-500 flex items-center gap-1">
                <span className="w-1.5 h-1.5 bg-blue-500 rounded-full"></span>
                Litros acumulados
              </p>
            </div>

            {/* Card 3: Machos */}
            <div className="bg-white rounded-xl shadow-sm p-6 border border-[#E8E8E8] hover:shadow-md transition-all duration-300 animate-fade-in">
              <div className="flex items-center justify-between mb-2">
                <p className="text-sm font-semibold text-earth-500 uppercase tracking-wide">Machos</p>
                <div className="bg-indigo-50 p-2 rounded-lg">
                  <span className="text-2xl">♂️</span>
                </div>
              </div>
              <p className="text-4xl font-bold text-indigo-700 my-3">
                {stats?.bySex.find(s => s.sex === 'MALE')?._count || 0}
              </p>
              <p className="text-xs text-earth-500 flex items-center gap-1">
                <span className="w-1.5 h-1.5 bg-indigo-500 rounded-full"></span>
                Reproductores
              </p>
            </div>

            {/* Card 4: Hembras */}
            <div className="bg-white rounded-xl shadow-sm p-6 border border-[#E8E8E8] hover:shadow-md transition-all duration-300 animate-fade-in">
              <div className="flex items-center justify-between mb-2">
                <p className="text-sm font-semibold text-earth-500 uppercase tracking-wide">Hembras</p>
                <div className="bg-pink-50 p-2 rounded-lg">
                  <span className="text-2xl">♀️</span>
                </div>
              </div>
              <p className="text-4xl font-bold text-pink-700 my-3">
                {stats?.bySex.find(s => s.sex === 'FEMALE')?._count || 0}
              </p>
              <p className="text-xs text-earth-500 flex items-center gap-1">
                <span className="w-1.5 h-1.5 bg-pink-500 rounded-full"></span>
                Productoras
              </p>
            </div>
          </div>

          {/* Por Categoría */}
          <div className="bg-white rounded-xl shadow-sm p-8 mb-8 border border-[#E8E8E8] animate-fade-in">
            <h3 className="text-2xl font-bold text-[#1a2e02] mb-6 flex items-center gap-3">
              <span className="bg-[#d3dbb8] p-2 rounded-lg text-2xl">📊</span>
              <span>Distribución por Categoría</span>
            </h3>
            
            <div className="grid grid-cols-2 md:grid-cols-5 gap-4">
              {stats?.byCategory.map((cat) => (
                <div 
                  key={cat.category} 
                  className="text-center p-5 bg-[#e8f0d8] rounded-xl border-2 border-[#d3dbb8] hover:border-[#6b7c45] hover:shadow-md transform hover:scale-105 transition-all duration-300 cursor-pointer"
                >
                  <p className="text-xs font-bold text-earth-600 uppercase tracking-wider mb-2">{cat.category}</p>
                  <p className="text-3xl font-bold text-[#1a2e02]">
                    {cat._count}
                  </p>
                </div>
              ))}
            </div>
          </div>

          {/* Por Raza */}
          <div className="bg-white rounded-xl shadow-sm p-8 mb-8 border border-[#E8E8E8] animate-fade-in">
            <h3 className="text-2xl font-bold text-[#1a2e02] mb-6 flex items-center gap-3">
              <span className="bg-teal-50 p-2 rounded-lg text-2xl">🧬</span>
              <span>Distribución por Raza</span>
            </h3>
            
            <div className="grid grid-cols-2 md:grid-cols-4 gap-5">
              {stats?.byBreed.map((breed) => (
                <div 
                  key={breed.breed} 
                  className="text-center p-6 bg-[#e8f0d8] rounded-xl border-2 border-[#d3dbb8] hover:border-[#6b7c45] hover:shadow-md transform hover:scale-105 transition-all duration-300 cursor-pointer"
                >
                  <p className="text-sm font-bold text-[#1a2e02] mb-3">{breed.breed}</p>
                  <p className="text-4xl font-bold text-[#6b7c45]">
                    {breed._count}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </div>
      </main>

      {/* FOOTER GRANME */}
      <footer className="bg-[#1a2e02] text-white py-8 mt-auto">
        <div className="max-w-7xl mx-auto px-6 text-center">
          <p className="text-sm text-gray-300 flex items-center justify-center gap-2 flex-wrap">
            <span className="font-medium">© 2024 CAPRI</span>
            <span>•</span>
            <span className="flex items-center gap-1">
              <span>🌾</span>
              <span>Sistema Profesional de Gestión Caprina</span>
            </span>
          </p>
        </div>
      </footer>
    </div>
  );
}
